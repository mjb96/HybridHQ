<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Enforce HTTPS for all traffic in production. -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Allow cleartext HTTP to the emulator host (10.0.2.2) and localhost so
         developers can use the http://10.0.2.2:8080 APP_URL override in debug
         builds without modifying this file. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
